export default {
  callback: 'A função de retorno de uma chamada (callback function) para que invoque',
  debounce: 'Tempo de duração para que espere antes que invoque o método de retorno de uma chamada (callback method)'
}
